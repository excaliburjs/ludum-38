// Place your settings in this file to overwrite default and user settings.
{
	"editor.tabSize": 3,
	"editor.insertSpaces": true,
	"files.exclude": {
		"**/.git": true,
		"game/*.js": { "when": "$(basename).ts" },
		"node_modules/": true
	}
}